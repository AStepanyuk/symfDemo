<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
            <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />


    </head>
    <body>


    <div class="container">

        <div>
            {% if auth.user %}
                current user: <b>{{ auth.user.name }}</b>
                <a href="{{ path('auth_logout') }}">Выйти</a>

            {% else %}
                <a href="{{ path('user_index') }}">Войти</a>
            {% endif %}

|
                <a href="{{ path('project-list') }}">Проекты</a>
                <a href="{{ path('user_index') }}">Пользователи</a>
                <a href="{{ path('reward_index') }}">Награды</a>


            <hr/>
        </div>

        {% block body %}{% endblock %}
    </div>
        {% block javascripts %}{% endblock %}

    <script src="/js/main.js"></script>

    </body>
</html>
